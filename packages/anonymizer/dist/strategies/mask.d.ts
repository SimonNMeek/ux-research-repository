import { BaseStrategy } from './base';
import { PIIMatch } from '../types';
export declare class MaskStrategy extends BaseStrategy {
    name: string;
    apply(match: PIIMatch, context: string): string;
}
//# sourceMappingURL=mask.d.ts.map